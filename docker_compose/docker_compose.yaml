version: '3.8'
services:
  web:
    build: .
    ports:
      - "5002:5002"
    environment:
      - DB_PASSWORD=${DB_PASSWORD} 
    depends_on:
      - mydb
  mydb:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      
      
    